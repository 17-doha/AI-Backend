# ─────────────────────────────────────────────────────────────
#  AI Agent Platform — Environment Variables
# ─────────────────────────────────────────────────────────────

# SQLite database path (file created automatically on first run)
DATABASE_URL=sqlite+aiosqlite:///./agent_platform.db

# OpenAI API Key (required)
OPENAI_API_KEY=sk-...

# OpenAI TTS voice (alloy | echo | fable | onyx | nova | shimmer)
TTS_VOICE=alloy

# App environment: development = auto-creates DB tables on startup
APP_ENV=development
